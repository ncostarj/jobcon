version: '3'
services:
  rpa:
    build: .
    container_name: rpa
    ports:
      - 8000:8000
    volumes:
      - .:/var/www/html/rpa-jobcon
    restart: unless-stopped
    # command:  npm start --prefix api
    networks:
      lan:
# volumes:
#   esdata1:
#     driver: local
  # esdata2:
  #   driver: local
networks:
  lan:
