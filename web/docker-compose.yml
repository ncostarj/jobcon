services:
  jobconweb:
    image: nginx:latest
    container_name: jobconweb
    hostname: jobcon.homelab
    working_dir: /var/www/newton/jobcon
    ports:
      - "8083:80"
    volumes:
      - .:/var/www/newton/jobcon
      - ./docker/config/nginx/sites-available:/etc/nginx/sites-available
      - ./docker/config/nginx/sites-enabled:/etc/nginx/sites-enabled
      - ./docker/config/nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - jobconapp
    restart: unless-stopped
    networks:
      - homelab

  jobconapp:
    build: 
      context: .
      dockerfile: ./docker/config/php/82/Dockerfile
    container_name: jobconapp
    working_dir: /var/www/newton/jobcon
    volumes:
      - ./docker/config/php/php.ini:/usr/local/etc/php/php.ini
      - .:/var/www/newton/jobcon
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
    restart: unless-stopped
    networks:
      - homelab

networks:
  homelab:
    external: true
    name: homelab_lan
